{
  "name": "fuel-src721-localnode",
  "version": "1.0.0",
  "description": "A simple local node setup.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "concurrently -k -r \"npm run build -- --watch\" \"nodemon dist/main.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "fuels": "^0.91.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.4",
    "typescript": "^4.7.2"
  }
}
